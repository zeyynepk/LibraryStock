namespace LibraryStock.App.Models
{

    // enum: bir değişkenin alabileceği sınırlı ve isimlendirilmiş sabit değerler kümesi genelde int tabanlı tip

    public enum Role
    {
        Admin = 1,
        Personel = 2
    }
}
